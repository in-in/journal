block append templates
  template#main-menu
    nav.menu
      ul.menu__list
        li(
            v-for='(i, index) in items',
            :style='{order: index + 1}'
          ).menu__item
          a(href='#').menu__link {{ i[0] }}
