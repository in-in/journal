block append templates
  template#app-button
    button(type='button').app-button
      i.app-button__wrap
        span(:class='{ "hide": hide }').app-button__text
          slot
        slot(name='app-button-icon')
        //- app-icon(name='thumb', :w=6, :h=2)
        //- +icon(thumb, 73, 66).card__icon
